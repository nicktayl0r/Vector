<ul>
    <% @orders.each do |order| %>
        <li> <%= link_to order.order_no, order_path(order) %> ----- <%= link_to "delete", order_path(order), method: :delete %> </li>
    <% end %>
    <%= link_to "return to admin dashboard", admin_path %>
</ul>